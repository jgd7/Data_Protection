// SECTION 2: RSA library

The folder contains:
	- RSALibrary.java: class completed with the implementation of the aforementioned functionalities.
	- RSALibrary.class
	- TestRSA.java : Main testing the Encryption, Decryption, Sign and Verification implementations.

To test the implementation, use TestRSA.java specifying the plaintext as argument. The plaintext cannot be longer than the RSA key (1024 bits).